name: mi_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  uuid: ^3.0.7
  flutter_local_notifications: ^17.2.3     # actualizada
  flutter_phoenix: ^1.1.0
  flutter_svg: ^2.0.9
  timezone: ^0.9.2                         # añadido
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/escudo.svg
    - assets/marca.svg

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon.png"
